# M3.1 Canvas Contract for DOCX

## Requirement
Every DOCX workflow output must be viewable through the Vulkan render surface.

## Contract
- Document content is transformed to render primitives.
- Render primitives are submitted through shared canvas pathways.
- Unsupported features degrade gracefully with visible fallbacks.
