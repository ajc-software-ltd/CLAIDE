# M3.6 — Render Canvas Unification

All document/media outputs should pass through shared Vulkan render-surface contracts.

## Goal
- Ensure DOCX/XLSX/SVG/font-driven text outputs are visible through the same canvas pathway.
